{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "build",
            "options": {
                "cwd": "src"
                },
            "command": "make", //"command": "make bin",
            //"problemMatcher": "$gcc"
        }
    ]
}